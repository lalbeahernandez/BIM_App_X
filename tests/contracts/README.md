# Contract tests

Generate the live FastAPI OpenAPI document in CI and diff semantically against `specs/openapi.yaml` once endpoint surface stabilizes. Add consumer contract tests for webhooks/integrations.
