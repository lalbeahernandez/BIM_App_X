# Visual regression

Capture stable UI shells, legends and Gantt. Do not snapshot stochastic 3D antialiasing unless the renderer is configured deterministically; compare semantic selection state separately.
