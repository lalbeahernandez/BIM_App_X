# E2E plan

Playwright scenario: create project -> create model -> upload `fixtures/ifc/tiny.ifc` -> wait revision READY -> open Work Area -> select element/BOQ/activity -> verify cross-highlight. Add after production viewer adapter is selected.
