# Security tests

Automate authorization-matrix tests first. In staging run DAST (e.g. ZAP) and container/IaC/dependency scanning. Add malicious archive/IFC corpus under private security fixtures; never publish weaponized samples casually.
