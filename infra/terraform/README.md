# Terraform skeleton

Intencionadamente cloud-neutral en el root. Crear módulos/provider específicos por organización: network, managed PostgreSQL/PostGIS, Redis, object storage, container runtime, secrets, observability and WAF.

No aplicar este skeleton directamente a producción sin threat model, sizing, backup/DR y revisión de costes.
