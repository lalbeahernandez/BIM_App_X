# Kubernetes / self-hosted

Prefer managed container platform until Kubernetes is justified. For self-hosted enterprise, add Helm/Kustomize manifests with separate API and worker autoscaling, NetworkPolicies, PodSecurity, external PostgreSQL/object storage, secrets integration and resource limits.
