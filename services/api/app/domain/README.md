# Domain modules

Move business logic here as features grow. Do not let routers become the domain. Suggested packages: `tenancy`, `models`, `boq`, `scheduling`, `progress`, `issues`, `qa`, `integrations`.
