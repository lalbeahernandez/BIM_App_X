{
  "schema_version": 1,
  "source_fixture_id": "DOMAIN-SCHEMA-001",
  "generator": "manual-static-summary@AUD-007",
  "generated_from_path": "db/init/010_schema.sql",
  "expected": {
    "table_count": 24,
    "index_count": 5,
    "required_tables": [
      "organizations",
      "projects",
      "models",
      "model_revisions",
      "bim_elements",
      "element_lineage",
      "boq_items",
      "activities",
      "progress_records",
      "audit_events",
      "outbox_events"
    ],
    "required_invariants": [
      "UNIQUE (organization_id, code)",
      "UNIQUE (model_id, revision_no)",
      "UNIQUE (revision_id, global_id)",
      "CREATE TABLE IF NOT EXISTS element_lineage",
      "CHECK (status IN ('QUEUED','PROCESSING','READY','FAILED'))"
    ]
  }
}
