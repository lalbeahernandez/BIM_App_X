# Fixtures

Sólo datos sintéticos o con licencia explícita. `tiny.ifc` es generado para este harness y no contiene IP de terceros.

Los golden datasets de clientes deben vivir en storage privado, nunca en repos públicos.
